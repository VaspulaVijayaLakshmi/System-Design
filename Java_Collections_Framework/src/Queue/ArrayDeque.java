package Queue;

public class ArrayDeque {

//    The Deque interface is part of the java.util package and extends the Queue interface. It stands for Double-Ended Queue and represents a linear collection that allows insertion, removal, and retrieval of elements from both ends.
//    Flexible Data Structure: Can be used as a stack (LIFO) or a queue (FIFO).

//    Flexible Data Structure: Can be used as a stack (LIFO) or a queue (FIFO).


//    Deque<String> d = new ArrayDeque<>();
//
//      d.addFirst("1");
//        d.addLast("2");

//    String f = d.removeFirst();
//    String l = d.removeLast();



//    Queue doesn’t use an array or list by itself; the underlying data structure depends on the implementation.


//    ArrayDeque → uses a resizable circular array
//    PriorityQueue → uses an array-based heap
//
//    ConcurrentLinkedQueue → uses a linked list
//    ArrayBlockingQueue → uses a fixed-size array
//    LinkedBlockingQueue → uses a linked list

}
