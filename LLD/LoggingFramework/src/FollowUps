Can, in the logging frame, which we log, and do we have different levels of logging? 
Can you confirm if we have different levels
of logging like information log or debug log or error log?


DEBUG – detailed information useful during development/debugging
INFO – normal application events
WARN – something unexpected but the application can continue
ERROR – an operation failed or an error occurred
FATAL – severe failure that may require application shutdown (less commonly used in modern frameworks)

____________________________________________


"What is the expected scale of the logging system? For example, how many logs per second do we expect,
and how large can the log volume be?"

Let's assume this is a general-purpose logging framework that could be used by multiple applications. 
We should support moderate to high log volume, potentially thousands of log messages per second.


That's the typical log-level threshold behavior.

For example, if the configured level is INFO:

DEBUG  ❌ Not logged
INFO   ✅ Logged
WARN   ✅ Logged
ERROR  ✅ Logged
FATAL  ✅ Logged



Yes. We prioritize availability and throughput. It's okay if logs are processed asynchronously and become available in the backend with some delay. 
However, we should try not to lose logs unnecessarily.

____________________________________________



Application Threads
       |
       v
     Logger
       |
       v
 BlockingQueue<LogMessage>
       |
       v
  Worker Thread(s)
       |
       v
    Appenders
    /       \
Console     File



In this we will use bounded blocking quuee because 
Log may be a lot and this will keep griwoing and it may lead ot memeory exhaustion.

SO bounded queue.
Blocking quuee is thread safe quuqe - Used in prod-consumer problem.

SO the consuemr will not wait indefiently...
BlockingQueue<String> queue =   new LinkedBlockingQueue<>(100);





